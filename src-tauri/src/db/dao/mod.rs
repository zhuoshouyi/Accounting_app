// DAO（Data Access Object）模块
// 提供对数据库表的 CRUD 操作
// Step 1 为骨架，后续 Step 逐步实现各表的 DAO

// 后续 Step 将添加以下 DAO 模块：
// - transactions.rs  — 交易明细 CRUD
// - account_owners.rs — 归属人 CRUD
// - category_tags.rs  — 消费标签 CRUD
// - category_rules.rs — 分类规则 CRUD
// - summary_mappings.rs — 汇总映射 CRUD
// - monthly_manual_data.rs — 月度手动数据 CRUD
// - import_records.rs — 导入记录 CRUD
// - ai_learning_rules.rs — AI 学习规则 CRUD
// - ai_reports.rs — AI 报表 CRUD
// - app_settings.rs — 应用设置 CRUD
